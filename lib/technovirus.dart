import 'package:flutter/material.dart';

class TechnoVirus extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('TechnoVirus'), backgroundColor: Color(0xFFFF1744)),
      body: Center(
        child: Text("This page is still in process"),
      ),
    );
  }
}
