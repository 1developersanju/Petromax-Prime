import 'package:flutter/material.dart';

class ViralVirusIntro extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(backgroundColor: Color(0xFFFF1744)),
      body: Center(
        child: Text("No video is added"),
      ),
    );
  }
}
